继续做 Obsidian 插件 `margin-notes-hz`(原名 `margin-notes`,已改名,见下方"事故记录"),路径:
`/Users/heptazero/Documents/my-obsidian/.obsidian/plugins/margin-notes-hz/`

## 背景

这是脚注/边注渲染插件,和同一个库里的 `everything-bilink` 插件(PDF 区域/文字选区双链、
笔记块引用)是姐妹项目但完全独立——`everything-bilink` 管"引用/链接",`margin-notes-hz` 管
"批注怎么显示",以后可能也给 PDF 批注复用这套渲染层。两个插件互不依赖。也和浏览器扩展
`margin-notes-web`(`/Users/heptazero/Documents/margin-notes-web/`)是同一构想的姐妹项目。

## ⚠️ 事故记录(2026-08-03,别重蹈覆辙)

manifest id 原来叫 `margin-notes`,和 Obsidian 社区商店里一个真实已发布的插件(作者
Abdul Majeed,`github.com/MajeedBaloch`,一个更重的"AI 批注 agent"插件,和这个项目功能
思路完全不同——它用自己的 chip 锚点系统,不碰 `[^id]` 脚注语法,还要填 OpenAI/Anthropic/
Gemini 的 API key 把笔记内容发出去)**id 撞车**。Obsidian 检查更新只按 id 匹配,不管
插件是本地开发的还是商店装的——它把本地这个 0.1.0 判定成那个商店插件的旧版本,用户一点
更新,**整个插件文件夹被替换**(不是只换 main.js),原来的 `.git` 仓库、`HANDOFF.md`、
全部 `src/` 源码都被删了,换成了别人插件的文件。

**教训,记死**:自建 Obsidian 插件的 manifest id **绝不能用容易撞车的通用名字**
(`margin-notes` 这种描述性短语大概率已经被别人用过)。改成了 `margin-notes-hz`
(带上作者标识后缀)。以后再开新插件,起 id 前先去 Obsidian 社区插件商店搜一下有没有
重名。

本次事故里 `src/*.ts`、`main.ts`、`styles.css`、`HANDOFF.md`、`package.json`、`manifest.json`
的内容都是从当时那次对话的上下文里原样重建的(逐字节一致);但 `tsconfig.json`、
`esbuild.config.mjs`、`versions.json`、`package-lock.json`、原来的两次 git 提交历史——这些
之前没在对话里完整读取过原文/无法从对象存储找回,是按 Obsidian 插件的标准写法重新生成的,
功能等价但不是原字节。git 历史从这次重建的提交重新开始。

## 现状:Phase 1-4 全部已实现,均在真实 Obsidian UI 里验证通过

**Phase 1(定位)**:`[^1]` 边注框在 Live Preview 下正确出现在正文右侧、跟对应引用同一行、
多个引用同时正确渲染,console 无报错。修复过 CM6 的经典坑:`rebuild()` 不能在
`ViewPlugin.update()` 里同步调用 `view.coordsAtPos()`(CM6 会抛
`Error: Reading the editor layout isn't allowed during an update`),必须用
`view.requestMeasure({ read, write })`。

**Phase 2-4**:边注框用 **Obsidian 原生渲染**(`MarkdownRenderer.render`)显示脚注定义——
LaTeX/MathJax 公式、加粗、双链等都走 Obsidian 自己的管线;无定义的引用显示斜体占位提示。
点击框 → 切换成**源码编辑模式**(纯文本 contentEditable,和 Live Preview 心智模型一致);
Enter 或点别处提交(写回文档底部 `[^id]: ...` 行)后切回渲染态,Esc 放弃修改;编辑一个
"无定义"的框会在文末自动追加新定义。相邻框按真实渲染高度依次下推,间距 8px(渲染是异步
的——MathJax 排版完才知道真实高度,避让在渲染 Promise 全部 resolve 后才跑,带 generation
计数防串台)。命令面板里有"清理无引用的脚注定义"命令,删掉所有正文里已无 `[^id]` 引用的
定义块。

## 代码结构

- `src/footnote-scan.ts` — 纯文本扫描:`[^id]` 引用 + `[^id]: ...` 定义块(含多行续行,
  tab 或 2+ 空格缩进,块内空行仅在后面还有续行时计入)。`FootnoteDef` 带 `from` /
  `markerEnd`(冒号后)/ `to`(块尾,不含换行)/ `content`(反缩进后的显示文本)。
  `serializeDefContent` 把多行文本序列化回定义语法(续行 4 空格缩进),与扫描互为逆运算。
- `src/collision-avoidance.ts` — `resolveCollisions<T>`:按自然 top 排序,前一个框的底部
  + minGap 往下推。支持变高框;泛型透传额外字段(比如 DOM 引用)。
- `src/margin-view-plugin.ts` — CM6 ViewPlugin,`createMarginNotesExtension(app)` 工厂
  (渲染需要 `app`)。流程:requestMeasure 第一趟测引用坐标 + 建框(自然位置)→ 异步
  `MarkdownRenderer.render` 渲染每个框(含 `$` 时先 `loadMathJax()`;`editorInfoField`
  拿 sourcePath 解析双链)→ 全部 resolve 后第二趟 requestMeasure 读 `offsetHeight` 做
  避让下推(`buildGen` 计数保证过期的异步渲染直接丢弃)。每轮 rebuild 换一个新的
  `Component` 托管渲染子组件,防泄漏。点击框 → `enterEdit` 切源码 contentEditable,
  blur/Enter 提交 → `view.dispatch` 写回 → docChanged 触发 rebuild 重新渲染;无改动时
  手动 `renderDisplay` 恢复渲染态;Esc 还原。编辑中若来了 rebuild 请求会挂起
  (`pendingRebuild`),blur 后再执行,避免编辑到一半 DOM 被重建摧毁。
- `src/main.ts` — 注册 editor extension + "清理无引用的脚注定义"命令(定义块从下往上删,
  保持前面 offset 有效)。
- `styles.css` — 边注框样式走 Obsidian CSS 变量(`--text-muted`、`--interactive-accent`
  等),跟随主题;layer `pointer-events: none`,框自身 `auto`。

## 接下来可以做的(按价值排序)

1. **确认重建后功能正常**:重载 Obsidian,写几个 `[^1]` `[^2]`、带 `$E=mc^2$` 的脚注,
   验证渲染/避让/编辑/孤儿清理都还在(应该没问题,代码是逐字节重建的,但值得过一遍)
2. 编辑中实时防抖写回(现在是 blur/Enter 才提交;要不要改看用户口味)
3. 性能:`update()` 里 viewportChanged 在滚动时也触发 rebuild,纯浪费(层随滚动免费移动
   是这个架构的核心卖点),可改成只在 docChanged/geometryChanged 时重建
4. 阅读视图(Reading view)支持——完全不同的渲染管线,要另接 MarkdownPostProcessor
5. 框内双链/普通链接点击目前会直接进入编辑态而不是跳转,要跳转得给链接元素单独拦 click
6. 同一 id 多处引用:现在框对齐第一个引用;可考虑 hover 高亮所有引用

## 一些已经踩过的坑,别重复掉进去

- **manifest id 别用通用名字,见上面"事故记录"**——这是本文件存在的直接原因
- `obsidian` npm 包对 `@codemirror/state`/`@codemirror/view` 有精确的 peer dependency
  版本要求,`package.json` 里**不要**自己再显式声明这两个包的版本,会和 npm 解析冲突
  ——让它们通过 `obsidian` 的依赖链传递获得类型定义就行(esbuild 里两者都在 external
  列表,不会被打进 bundle,运行时由 Obsidian 自己提供)
- 批注框的水平位置不能用 `left: 100%` 或固定 CSS 值,得用 `view.contentDOM` 的
  `getBoundingClientRect()` 动态算——很多主题会把正文内容居中显示在比编辑器窄的一栏
  里,两侧留白,批注框应该贴着"正文实际渲染宽度"的右边,不是贴着编辑器整个滚动容器
  的右边
- `ViewPlugin.update()`(以及插件构造函数,它本质也在初次 update 周期里)里不能直接同步
  调用 `view.coordsAtPos()` / `element.getBoundingClientRect()` 这类布局读取——CM6 会抛
  `Error: Reading the editor layout isn't allowed during an update`,且这个报错不会中断
  编辑器本身,只会安静地让你的 `update()` 提前 throw、DOM 操作全部没跑,表现就是"什么都
  没发生",很容易被误判成定位算法错了。正确做法是用 `view.requestMeasure({ read, write })`
  ——`read` 阶段做所有布局测量,`write` 阶段做纯 DOM 写入,两者严格分离
- 用 computer-use 操作 Obsidian UI 测试时,系统中文输入法会把字母吞进拼音组合、把
  cmd+P 之类的按键打进笔记里,极易误触。能 headless 验证的(纯逻辑单元测试、构建、读
  文件)一律 headless;真要 UI 验证,让用户自己动手,或者先切英文输入法
- `el.innerText.replace(/ /g, " ")` 里的 ` ` 必须写成显式转义,不能指望编辑器
  /终端正确处理字面 NBSP 字符——本次重建时手滑漏打字面 NBSP 导致正则变成 `/ /g` 的空操作,
  一开始没发现

## Phase 5:PDF 便利贴批注,2026-08-16(v0.4.0)

在 Phase 1-4 的脚注边注渲染之外,新增一套独立功能:在 PDF 上写批注,不经过笔记文件、
不走 wikilink 反链系统,是"锚定在某页某处的一张本地便利贴"。和脚注渲染共用
`src/collision-avoidance.ts`。

### 核心模型:一种东西,两个正交开关

早期版本用 `kind: "floating" | "margin"` 区分两种批注,**这是个设计错误**——它把两个
互相独立的维度硬捆在一起,导致"悬浮的永远是点、侧边的永远是展开"。v0.4.0 拆成:

|            | `collapsed: false` | `collapsed: true` |
|------------|--------------------|-------------------|
| `pinned: true`  | 侧边轨道里的框 | 轨道里的一个点 |
| `pinned: false` | 自由摆放的便利贴 | 页面上的一个点 |

四格都有意义,随时可以互相切换(框上的工具条 / 右键菜单)。`normalizeAnnotation()`
负责把旧记录升级:`floating` → free+collapsed,`margin` → rail+expanded。

### 数据存哪里

库内一个 JSON 文件,路径可在设置里改,默认 `.margin-notes-hz/`。
**不放插件自己的 `data.json`**:本库 `.gitignore` 排除了整个 `/.obsidian/plugins/`,
放那儿的批注不会进 git;Obsidian Sync 也把插件数据当成要单独勾选的类别。

**踩过的坑**:路径设置最初只当文件路径处理,用户照着旁边 `branch-timeline` 的样子填了
`99_assets/plugin-data/margin-note`(明显是想要文件夹),结果那里被建成空目录、批注根本
没写进去,还留在旧位置。现在 `resolveDataFilePath()` 会判断:以 `.json` 结尾当文件,
否则当文件夹、在里面放 `annotations.json`。设置页会实时显示"实际文件:<路径>"。

```ts
interface PdfAnnotation {
	id: string; page: number;
	anchor: PdfRect;              // 指向哪 —— PDF point,来自选区/框选
	pinned: boolean;              // 在轨道里 or 自由摆放
	collapsed: boolean;           // 收成点 or 展开
	side: "left" | "right";       // 哪条轨道
	freeX?, freeY?, freeW?, freeH?;  // 自由摆放:页面百分比(可为负/超 100 = 页面外)
	offsetY?: number;             // 轨道:手动上下微调(px)
	fontScale?: number;           // 单条字号倍率
	text: string;                 // Markdown 源码
	createdAt, updatedAt: number;
}
```

**为什么自由坐标用百分比而不是像素**:百分比是相对页面框的,缩放时便利贴自动跟着页面走,
不会跑飞;而且允许 <0 或 >100,天然表达"贴在页面左右外面的空白里"。
**为什么 `offsetY` 和 `anchor` 分开**:`anchor` 永远表示"指着原文哪里",`offsetY` 表示
"用户想让框显示在哪",混在一起的话以后做重新锚定/导出引用原文时锚点已被拖动污染。

### 定位:两条踩过的坑

**1. 轨道必须钉在可视区,不能钉在页面边。** 最初 `left = pageRight + GAP`,PDF 放大到
填满窗格时轨道就跑到视口外面,只能缩到很小才看得见。现在:
```
右轨道 left = min(页面右边 + GAP, 可视右边 - 轨道宽 - GAP)
左轨道 left = max(页面左边 - 轨道宽 - GAP, 可视左边 + GAP)
```
留白够就贴着页面(离正文近好读),不够就压到页面上,**永远可见**。

**2. 轨道批注固定 px,自由便利贴跟着缩放。** 轨道是 UI chrome,尺寸应该屏幕固定,否则
PDF 缩小时批注框会相对页面越来越大、喧宾夺主(用户原话:"无论我缩多小这个就跟着放大")。
自由便利贴是"贴在纸上的",宽高字号都随页面缩放。字号:轨道 = `fontSize`,
自由 = `fontSize × zoom`,各自再乘 `fontScale`。

**3. 只有一个层,不再分两层。** 早期悬浮批注画在 `getOverlayLayer()` 的页内覆盖层里,
那层 `overflow: hidden`,导致便利贴一旦放到页面外就被裁掉——这正好挡死了"放在左右空白处"
这个核心需求。现在所有批注(包括点)都画在滚动容器下的**同一个层**里,位置由页面
`getBoundingClientRect()` 换算,`overflow` 不再裁任何东西。页内覆盖层现在只剩框选时
画虚线框在用。

### 代码结构(`src/pdf/`)

- `pdfjs-types.ts` / `pdf-layer.ts` / `selection-geom.ts` / `rect-select.ts` ——
  **从 `everything-bilink/src/` 原样镜像**(不抽共享包,理由见文件头注释)。以后 Obsidian
  升级打断这条未公开内部链路,**两个插件都要改**。
- `annotation-types.ts` —— 数据模型 + `normalizeAnnotation()` 旧记录升级。
- `annotation-store.ts` —— 持久化、debounce 落盘、路径解析、PDF 改名跟随
  (`renameFile` 挂 `vault.on("rename")`;纯路径 key 否则文件一移动批注就悄悄丢)、
  多级迁移(新路径 → 旧默认文件夹 → 更旧的插件 data.json)。JSON 坏了**抛错不清空**。
- `annotation-anchor.ts` —— 文字选区 → 锚点矩形。是 `everything-bilink` 的
  `text-select-copy.ts::readActiveSelection` 简化版:只要几何不要文字,所以没有公式回填。
- `annotation-box.ts` —— 框壳。显示态走 `MarkdownRenderer.render`(LaTeX/双链/加粗全支持,
  和脚注框同一套管线);点击切源码 contentEditable,blur/Enter 提交、Esc 放弃。
  **渲染出的 `a.internal-link` 自己挂了 `openLinkText`**——Obsidian 只在它自己的视图里
  接管这类点击;点链接跳转、点别处进编辑,靠 `closest("a")` 区分。
- `annotation-layer.ts` —— 唯一的渲染层,前面那三条定位逻辑都在这。工具条:拖动柄 /
  切换左右轨道 / 钉住↔自由 / 收起成点 / 删除;右键菜单额外有字号增减、恢复自动高度。
  轨道批注的 resize 手柄拖的是**共享的轨道宽度**(存进设置),自由便利贴拖的是自己的宽高。
- `annotation-list-view.ts` —— 右侧栏面板,按页码+页面纵向位置列出全部批注(渲染 Markdown),
  点击跳到那页并闪烁。
- `controller.ts` —— 扫描 PDF 视图、每视图一份 `ViewState`、命令入口。**批注初始形态由
  调用的命令决定**(之后随时可改),不解析笔记语法猜。

### 命令 / 入口

- `[PDF] 加批注:右侧轨道` / `左侧轨道` / `自由摆放(便利贴)`
- `[PDF] 打开批注列表面板`(另有 ribbon 图标)

有选中文字就直接锚上去;没选中就提示拖一个框标出位置(这就是"框选",之前完全没提示,
用户反馈看不懂)。

### 验证状态

- **已验证(headless)**:store 层 15 个用例(路径解析文件/文件夹两种写法、旧
  `kind` 迁移、旧位置迁移、新字段往返、损坏文件抛错、upsert/remove/forPage、
  renameFile 合并、onChange 订阅);`npm run build` 干净。测试脚本在会话 scratchpad,未入库。
- **完全没验证**:所有交互——拖动、缩放、钉住/自由切换、收起展开、轨道宽度拖拽、
  字号右键调整、Markdown/LaTeX/双链渲染与跳转、列表跳转闪烁、
  `findScrollAncestor` 是否真找到 pdf.js 的滚动祖先。

### v0.4.1 修的几个 UI bug(用户实测反馈)

1. **字号没有入口** —— 只做在右键菜单里,没人找得到。现在工具条直接放了 A−/A+,
   外加一个 `⋯` 按钮打开同一个菜单(右键菜单保留)。按钮缩到 15px 才塞得下 7 个,
   `MIN_RAIL_WIDTH` 相应提到 130。
2. **右侧轨道的缩放手柄拖起来像在拖右边** —— 手柄位置本来就对(在朝向页面那一侧),
   但拖动时只改了 `width`、没改 `left`,而右轨道的 `left` 是固定的,于是框往右长,
   看着就像在拖另一边。现在被抓住的那条边跟着鼠标走(`grabsLeftEdge` 时同步改 `left`)。
3. **缩放后批注漂移(滚动不漂)** —— 两个原因叠加:pdf.js 缩放时会**每页**发一次
   `pagerendered`,每次都触发 rebuild,而那时其他页还没重新排版完,量到的是旧矩形;
   而且原来的实现在 `await` Markdown 渲染**之前**就把几何量好了,渲染完直接用旧数字定位。
   现在 rebuild 有 60ms 防抖合并这一串事件,并且拆成 `layout()` 独立函数,**渲染前后各
   跑一次**——第二次重新测量当前页面矩形。拖动结束时也改成现场重新量页面框,
   而不是用建框时捕获的那份。
4. **批注面板点了没反应、只显示"当前没有打开的 PDF"** —— 根因:面板一被点击就成了
   "活动视图",`getActivePDFView()` 当场返回 null。现在 controller 记住
   `lastPdfView`(还开着才算数),面板和跳转都用它;面板自身获得焦点时跳过重渲染。
   跳转也改成复用 PDF 已在的那个 leaf(`setState` 带 `#page=`),不再在侧边栏旁边
   另开一份。
5. 面板底部换成 `--background-primary` 并 `min-height: 100%`,不再露出侧边栏那层
   不同底色的色带。

### v0.5.0 —— 重新定义"固定",加颜色/纯文字样式,批注面板可编辑跳转(用户实测反馈)

**1. "固定"的语义反了,推翻重做。** 0.4.x 把"固定"实现成屏幕固定尺寸的 UI chrome
(不随 PDF 缩放变化)。用户纠正:这不是他们的原意——"固定"应该和便签("自由"那种)
一样贴在 PDF 上、随缩放等比例变化,唯一的区别只是**横向被限制在左右两条轨道里**,
不能随便摆。现在 `PageBox` 多存了 `ptX0/ptX1/ptWidth`,`settings.railWidth` 的语义
从"px"改成"PDF point(100% 缩放时的 px)",渲染时统一 `* box.zoom` 换算成当前
屏幕像素——轨道宽度、批注宽度、字号,固定和自由现在走同一套缩放公式,只有 X 坐标的
算法不同(轨道用 `railLeft()` 卡边,自由用 `freeX/freeY` 百分比)。拖轨道内边缘调宽度时
落盘前除以当前 zoom,换回"point"存,保证以后缩放时视觉宽度还是用户当时选的那个。

**2. 批注面板:分页折叠 + 面板内直接编辑/删除 + 跳转带高亮。**
`annotation-list-view.ts` 整个重写:
- 按页码分组,每组一个可折叠 header(点头部展开/收起,状态存在 view 实例上,
  面板关闭重开会重置——这是会话态,不值得落盘)。
- **每一行直接复用 `buildAnnotationBox`**(和页面上的批注同一个组件),而不是只读的
  `MarkdownRenderer.render`——所以面板里点正文能直接编辑,工具条上有跳转和删除两个
  按钮,不用打开 PDF 就能改/删批注。
- 跳转(`revealAnnotation` → `AnnotationLayer.reveal()`)除了闪一下批注框本身,
  现在还会在**原文锚点位置**画一个临时高亮矩形并 `scrollIntoView`——批注框自己在轨道里
  或者被拖到别处,并不代表那就是原文位置,用户要看到的是"这条批注指的到底是哪句话"。

**3. 颜色 + 纯文字样式,工具条压缩成一个"⋯"。**
`PdfAnnotation` 加了 `color?` 和 `style?: "boxed" | "plain"` 两个可选字段。
页面上批注框原来的工具条有 6 个图标(字号±、固定、收起、更多、删除)当场就把短批注的
文字糊住了——现在压缩成**只有拖动柄 + 一个"⋯"**,点开是同一个右键菜单(菜单本身没删减,
新增了"改成纯文字样式"和"更改颜色…"两项)。颜色用一个隐藏的原生
`<input type=color>` 触发系统取色器(和设置页 `addColorPicker` 背后是同一套机制),
选完立即生效、离开自动清理。纯文字样式去掉边框/背景/阴影,只剩带色文字,悬停或编辑时
临时露出一个底,方便点回去编辑。

### v0.5.1 —— 三个真 bug(用户实测反馈)

1. **批注面板点击跳转,静默失败。** `revealAnnotation` 对已经打开的 PDF leaf 调用了
   `view.setState({file, subpath}, ...)`——这是错的:`setState()` 是每种 View 自己的
   **持久化状态**格式,FileView 不认得 `{file, subpath}` 这个形状,调用直接是静默空操作。
   导航一个**已经打开**的文件到某个 subpath,正确 API 是 `View.setEphemeralState()`
   (`MarkdownView` 跳转到标题/块引用用的就是这个)。改用
   `existing.view.setEphemeralState({ subpath: "#page=N" })` 后修复。
2. **新建的自由便利贴默认出现在页面外面。** `freeXPct` 默认值原来写死 102%(页面右边缘
   往外一点点),不管选区在哪——页面在阅读器窗口里贴得紧、没什么留白的时候,这个位置
   经常落在可视区域外面,便利贴创建了但看不见。改成默认贴着**选区右边**
   (锚点右边缘 + 3%),不再存成固定值——因为只在 `freeX` 未设置时才用这个默认公式,
   每次渲染都重新按锚点算,不会像存死的 102% 那样"一直错在同一个地方"。
   `togglePin` 解除固定时也不再写死跳到 102%/-30% 的角落,同样交给这个默认公式,
   于是解除固定也会自然落在原来那条批注所在的位置附近。
3. **固定轨道拖宽度,拖一点就卡住。** 根因不在 resize 的算法本身,而在防抖:
   `rebuild()` 只在**调度**这次重建的那一刻检查了 `isBusy()`(是否有框正在编辑/拖拽),
   但 60ms 后真正执行的 `doRebuild()` 并不会再检查一遍——如果调度和执行之间用户开始拖动
   (很容易发生,比如拖拽引发的滚动会让 `onTextLayerReady` 再触发一次调度),
   `doRebuild()` 照样把整个层 `layer.empty()` 清空重建,用户手指下面那个 DOM 元素被换掉,
   但 `pointermove` 监听器还挂在 `window` 上继续触发,改的是一个已经不在文档里的旧元素
   ——看起来就是"拖一点就死了"。现在 `doRebuild()` 一开始、以及 Markdown 渲染
   `await` 结束后都会重新检查 `isBusy()`,忙碌就直接放弃这一轮(拖拽/编辑结束时的
   `refresh()` 会补上)。

### v0.5.2 —— 尺寸下限 + 两边都能拖(用户实测反馈)

1. **下限设得太大,而且 slider 和代码对不上。** `MIN_RAIL_WIDTH_PT` 是 130,而设置页
   slider 的最小值是 110 —— **slider 最下面那一段完全没作用**,拖到底也还是 130。
   现在:轨道 50pt(slider 50–500)、自由批注 40pt、字号 slider 6–28px、
   单条字号倍率 0.3–4x。下限只当"别塌成 0"的保险,不再替用户判断多窄算窄。
   **规矩:`MIN_*` 常量必须 ≤ 对应 slider 的最小值**,否则又会出现拖了没反应。
2. **只有一条边能拖,而且找不到。** 原来只在"朝向页面那一侧"放了一个手柄,而且定位是
   `left: -2px` —— 但批注框是 `overflow: hidden`,这个手柄有一部分直接被裁掉了,
   这是"不知道怎么拖"的直接原因。现在左右**两条边都有手柄**(自由批注另加右下角改高度),
   全部放在框内侧;鼠标悬停在批注上时手柄会显示一条淡色竖条,悬停在手柄上加深。
   抓哪条边哪条边跟着光标走(抓左边时同步改 `left`,否则看起来像在拖对面那条边);
   自由批注往左拉时会同时更新 `freeX`,保持右边缘不动。

### v0.6.0 —— 轨道定位彻底去掉滚动依赖(用户实测反馈)

**一个根因,三个症状。** `railLeft()` 原来是:
```
右轨道 = min(页面右边 + GAP, scrollLeft + clientWidth - 轨道宽 - GAP)
```
那个 `min(...)` 是为了"轨道永远在可视区内"加的,但它把**滚动状态**混进了定位公式,
于是同时坏了三件事:

1. **缩放时轨道漂移,自由便签不漂。** 用户自己就推理对了:"照理说这个跟自由便签
   是一样的逻辑啊"——对,自由便签只用页面几何,而轨道多依赖了 `scrollLeft`,
   缩放时滚动位置变了,轨道就跟着漂。
2. **轨道出不去 PDF 的范围。** 那个 clamp 的字面含义就是"不许超出当前视口",
   所以永远没法把批注放到页面右边更远的空白里。
3. **拖完轨道视野被拉到贴边 + 撞上滚动条。** 这是个自我喂食的回路:轨道撑大了
   滚动内容宽度 → `scrollLeft + clientWidth` 跟着右移 → 轨道又右移 → 视野被拖着走。
   而且它正好被摆在视口最右边,macOS 的覆盖式滚动条就在那个位置,一拖就碰上。

**改法**:`railLeft()` 变成**纯页面坐标**,和自由便签同一套逻辑——
`右 = 页面右边 + gap`、`左 = max(0, 页面左边 - 宽 - gap)`,gap 也按 point 存、乘 zoom。
唯一剩下的 clamp 是左轨道不许为负(内容原点左边滚不过去,会永远够不着)。
另外给图层设了显式 `width = 最右批注 + OUTER_MARGIN_PX(28)`,这样滚动容器能真的
滚到页面外的批注那里,而且最外侧批注右边留一段空白,不会贴着滚动条。

**代价说清楚**:放大到页面填满窗格时,轨道批注会在视口外面,要横向滚动才看得到。
这是用户明确要的取舍("固定不是屏幕视角,而是依附 pdf 的位置")——它现在真的就是
一张贴在纸上、只是横向对齐到某条泳道的便签。

**批注面板的按钮位置**:从右上角挪到**行右侧垂直居中**,悬停才显示(行又矮又宽,
角标读起来像是脱离了它所属的那一行)。右侧空位是**常驻**的,不在 hover 时才撑开
——否则鼠标一悬停正文就重排,手感很差。

### v0.6.1 —— 面板删除不同步 + 轨道只有一边能拖(用户实测反馈)

1. **在列表面板删除批注,PDF 上还在。** `store.onChange` 只有列表面板订阅了,
   `AnnotationLayer` 谁都没订阅——它只在 `pagerendered`/`textlayerrendered`/`resize`
   时重建。所以从面板删除/编辑,数据和面板都更新了,页面上那条纹丝不动,要等下次
   翻页重渲染才消失。现在 controller 在 `onload()` 里统一订阅一次
   `store.onChange → rebuildAll()`。图层重建本来就有 60ms 防抖、且拖拽/编辑中会跳过,
   所以多出来的这点触发是无害的。

2. **轨道只有一条边能拖,另一条"拖不动、只让对面变长"。** 这不是手柄坏了,是
   **几何上就不可能**——轨道批注的位置是**算出来的**(`railLeft()` = 页面边 + gap),
   不是存下来的,所以朝向页面那条边被布局公式钉死了,光改 `width` 根本推不动它:
   松手重排后它弹回原位,长出来的部分全跑到对面去。
   **真正的解法是意识到轨道有两个自由度,一条边各管一个**:
   - 外侧边(远离页面)→ 轨道**宽度** `railWidth`
   - 内侧边(朝向页面)→ 轨道**离页面的距离** `railGap`(新增设置),
     同时 width 反向补偿,保证外侧边不动
   两个都是整条轨道共享的设置,所以拖任何一条批注的边 = 改这一侧全部批注,
   这本来就是"轨道"的含义。`railGap` 允许为负 = 把轨道压到页面上。
   几何在 scratchpad 里用 7 个用例验证过(左/右轨道 × 内/外侧边 4 种组合都满足
   "抓哪条边哪条边跟光标走、对面那条不动",外加负 gap、宽度下限、左轨道空间不足
   被 clamp 到 0 这三种边界)。

3. **设置里"轨道宽度"是什么意思**——用户直接问了,说明文案没交代清楚模型。
   设置页现在先有一段总述:「固定」的批注不是自己记住位置,而是排在页面左右两条轨道里,
   所以宽度和离页面的距离是**整条轨道共用**的,单位是 PDF 点、跟着页面缩放。

### v0.7.0 —— 原文/译文自动配对,共用同一份批注

用户的工作流:论文会用管线翻译成**保留排版**的中文版。希望在原文和译文上看到
同一套批注,并且能一键切换语言、停在同一个位置。

**先测量再设计。** 拿库里真实的两对文件量过:

| 指标 | 结果 |
|---|---|
| 页数 / 页面尺寸 | 16 vs 16、14 vs 14,均 612×792 |
| 文本块位置 IoU 中位数 | 0.72 |
| **对应段落纵向偏移中位数** | **1.0pt** |
| p90 / 最大 | 9.8pt / 28.5pt |
| 落在一行高度内 | 93% |

结论:排版保留得足够好,**坐标可以直接共用**,不需要重新锚定。

**配对不是"同步两份数据",而是"本来就是一份"。** store 的 key 从「路径」改成
「组」:`路径 --pairs--> 组 key --> 批注数组`。原文和译文指向同一个组,所以增删改
天然一致,不存在同步冲突。组 key 用**原文那一侧的路径**(译文侧才是衍生物)。

**自动识别的规则(全部经过真实语料验证)。** 只按文件名匹配是**不安全**的:86 个 PDF
里名字包含关系能匹配出 24 对,其中只有 2 对是真的。假阳性包括三个同名
`hopfield_pooling.pdf`(不同子目录里的不同插图)、`foo` vs `foo_adapted`、
以及同一篇论文的两份英文副本。最终规则是四条同时成立:

1. 文件名归一化(去掉所有非字母数字)后互相包含,核心长度 ≥12、长度比 ≥0.6
   ——**不维护前后缀白名单**,因为用户的前后缀会变,但论文名一定在
2. 页数相同
3. 页面尺寸相同
4. **一侧 CJK 占比 ≥25%、另一侧不是**

第 4 条是决定性的,单靠它就排除了 22 个假阳性中的绝大多数。这四条跑下来,
在真实语料上**恰好只留下那 2 对真的**。

**指纹从哪来:全部白嫖自已打开的 viewer**,不额外加载任何 PDF——页数取
`pdfDocument.numPages`,页面尺寸取 `pageView.pdfPage.view`,CJK 占比取文字层
`textContentItems`(前 3 页采样)。代价是**两份都至少打开过一次**才会配对成功,
但这恰好也是能真正**校验**而不是靠文件名猜的时刻。

**切换命令**`[PDF] 切换到对应的译文/原文(保持页码和位置)`:取当前视口里占比最大的
那一页作为页码,再记下视口在该页内的纵向比例,打开对方后重新施加同一个比例。
批注面板顶部也会显示一条「与 xxx 共用批注」,点一下同样切过去。

**数据格式升到 v3**,新增 `pairs`(成员路径 → 组 key)和 `fingerprints`(路径 → 页数/
尺寸/CJK 占比)。v1/v2 文件读进来这两项就是空对象,不需要迁移。改名时 `pairs`、
`fingerprints`、批注三者一起跟着走,包括"被改名的正好是组 key"这种情况。
store 层 9 个用例已验证(含合并、双向增删、往返、改名、解除配对)。

### v0.7.1 —— 配对根本没生效的两个 bug(靠用户的真实数据文件诊断出来)

用户报告"01_PDF-trans 里的都试了不行"。**直接读他的 `annotations.json` 就看清了**:
`pairs: {}`,而 `fingerprints` 里**只有 1 条**,还是个无关的 64 页文档。两个独立的 bug:

1. **指纹几乎从不生成。** `ScriptSampler.done` 要求采满 **3 页**才写指纹,但 pdf.js
   只渲染视口内的页——打开一个 PDF 不往下滚,只会渲染 1~2 页,`done` 永远不为真,
   指纹永远不写,自然永远配不上。唯一成功的那条是因为用户滚动过那个长文档。
   **改成第一页就写,后续页到达时再细化**(`done` 现在只用来停止采样,不再控制写入)。
   `setFingerprint` 也跟着改:原来只比几何字段就提前 return,会把 `cjk` 永久钉死在
   第一页的值上。

2. **中文占比算出来是错的。** 那条唯一的指纹给一篇**纯英文**论文记了 `cjk: 0.20`,
   而 PyMuPDF 实测同样前 3 页是 `cjk=0 latin=9193 → 0.000`。阈值当时是 0.25,
   **离误判只差 0.05**。原因基本可以确定是 LaTeX 数学字体的 ToUnicode 映射损坏,
   glyph 落进了 CJK 码段。
   **改法:不再用绝对阈值,改成要求两侧的"差距"够大** —— `|a.cjk - b.cjk| >= 0.4`
   且 `max >= 0.5`。真实译文约 0.9,英文即使被噪声抬到 0.2 也照样分得开;而同语言的
   两份副本无论绝对值多少都因为差距为 0 被拒。
   顺带把取文本的方式从 `textContentItems`(在镜像的类型里声明了,但**代码里从来没有
   任何地方真正用过**,跨 Obsidian 版本是否存在未经验证)换成 `textDivs` 的
   `textContent` —— `getTextLayerInfo()` 本来就是靠 `textDivs` 判断可用性的。

3. **失败时不再只说"没找到"。** `explainNoCounterpart()` 会指出具体卡在哪一步:
   没有名字相近的文件 / 当前文件还没采到指纹 / 候选存在但从没打开过(最常见)/
   页数尺寸对不上(附上两边的实际数字)/ 两边看起来是同一种语言(附上两边的占比)。
4. **兜底:`[PDF] 手动关联原文/译文`** 走一个 FuzzySuggest 选择器(优先列名字相近的,
   没有就列全部 PDF),**跳过所有校验**直接配对;另有 `[PDF] 解除原文/译文关联`。
   自动识别再怎么调都可能有边角情况,得有一条不依赖启发式的路。

### v0.7.2 —— 切换语言时的过渡效果

用户问能不能做成平滑过渡。**如实说清楚了做不到的部分**:原文和译文是两份独立的
PDF 文档,pdf.js 在换文件时会整个拆掉重渲染页面 canvas,没有公开的办法让两份文档
同时保持渲染状态去做真正的内容 crossfade——那需要截取两边的 canvas 像素自己做动画,
成本和脆弱程度都不成比例。

**做了两件真实可行的**:
1. 切换瞬间给整个视图容器一个短暂的透明度回落(`.margin-notes-pdf-switching`,
   180ms),软化文件替换那一下的硬切,不是内容动画,只是个视觉缓冲。
   **这个类只挂在 `view.containerEl`(View 公开 API 的一部分)上,不猜 pdf.js
   内部的 class 名**——这插件已经因为假设未公开 DOM 结构吃过亏,这次直接避开。
2. 落到目标位置那步,从 `scrollTop +=`(瞬间跳)改成 `scrollBy({behavior:"smooth"})`
   (动画滚动)——这是真正在做动画的部分。

`view.containerEl` 是在切换前捕获的普通元素引用,不是从 `view` 或 `view.leaf`
延迟读取——同类型文件切换 Obsidian 会复用同一个 View 实例(`onLoadFit`/`onLoadFile`
那套生命周期,不会把整个 View 拆了重建),但拿着元素引用比每次都重新问 `view` 更稳。

### v0.8.0 —— AI 写批注:quote 锚点(补上一直悬着的那个 TODO)

早前讨论过、一直挂在"还没做"清单里的那条:`anchor: PdfRect` 是 PDF 坐标,
一个只读文本的 AI 根本推不出来,所以之前的 schema 对 AI 只读友好、写不友好。这版补上。

**新增可选字段 `quote?: string`。** `anchor` 缺省值本来就是 `[0,0,0,0]`
(`normalizeAnnotation` 里一直是这么写的),现在把它的含义定下来:**这是"待解析"的标记**。
渲染层(`annotation-layer.ts` 的 `resolveAnchorIfNeeded`)在每页第一次上屏时检查:
锚点是不是 `[0,0,0,0]`?有没有 `quote`?有就去这一页的文字层里搜,搜到就用
`selection-geom.ts` 现成的 `computeSelectionRects`(和用户手动划选区用的是同一套多行合并
逻辑)算出真实矩形,写回 store——**从此这条记录就是一条普通的、按坐标定位的批注**,
以后每次渲染都是走已有的快路径,不再重新搜索。

**新文件 `quote-anchor.ts`**:把文字层的 `textDivs` 拼成一个扁平字符串(相邻 span 间插
一个空格,pdf.js 常把一个词拆进好几个 span,`O(N`、`2`、`)` 各占一个 div 完全正常),
大小写不敏感、空白折叠后做子串查找,再把命中位置映射回 `(div 下标, 组内偏移)` 二元组,
喂给 `computeSelectionRects`。命中不到(quote 是转述而非原文摘录,或大小写/空白差异
大到折叠也救不回来)直接返回 `null`,不返回一个瞎猜的位置。

**没找到时的兜底,以及一个诚实的局限**:quote 没给、或者没搜到,批注会画在页面左上角
一个固定小块里(可见但位置不准),不持久化这个假位置——但因为是在同一个内存对象上直接
改的 `ann.anchor`(不是拷贝),`isUnresolvedAnchor` 下次就查不到"待解析"标记了,
所以**这次没解析成功,这个会话里就不会再重试**。实际影响很小:如果是"文字层还没渲染好"
这种竞态,60ms 防抖内几乎总能自己追上;如果是"quote 压根不在这页"(比如 AI 转述而非
原文摘录),重试多少次也不会成功,停在兜底位置反而是对的。真正的边角情况是介于两者之间
的极小概率窗口,值得知道但不值得为此再加一层机制。

**给另一个 AI 用的说明,直接抄这段给它:**

```
批注文件路径:.margin-notes-hz/annotations.json(或用户设置里的实际路径,
  Obsidian 设置 → margin-notes-hz → 批注数据 → 会显示"实际文件:xxx")

写入前先做两件事:
1. 读这个 JSON,找 pairs 字段(结构是 {成员PDF路径: 组key}) —— 如果你要标注的PDF
   路径出现在 pairs 里,批注要写进 pdfAnnotations[组key] 数组,不是
   pdfAnnotations[原始路径];组key通常是原文(非中文)那一侧的路径。这是为了
   和它的原文/译文对应版本共用同一份批注,写错桶对方看不到。
2. 确认 Obsidian 此刻没有打开(或提醒用户先关掉)——这个文件被插件用 debounce
   方式读写,同时改容易互相覆盖。

每条批注只需要写这些字段,其余会在下次打开 Obsidian 时自动补全默认值:
{
  "page": 4,
  "quote": "the network exhibits O(N^2) capacity",
  "text": "对比 [[Demircigil 2017]] 的指数容量结果"
}

quote 的要求:
- 从 PDF 正文原样摘录的一段连续文字(不是转述、不是你自己的总结),
  4 个字符以上,越长越准(但别整段抄,一行左右够精确定位了)
- 大小写、空白不敏感,但字符本身要和 PDF 里的一致
- 找不到就整条批注会出现在那一页左上角,不会报错也不会丢

text 字段就是批注正文,支持 Markdown(包括 [[双链]] 和 $LaTeX$),会用 Obsidian
自己的渲染管线显示。
```

**store 层没有新增测试**——这一版改动集中在渲染时的按需解析,不是持久化格式本身
(`quote` 只是 `PdfAnnotation` 上又一个透传字段,`normalizeAnnotation` 已有的模式直接
覆盖);字符串搜索/偏移映射那部分逻辑在 scratchpad 里单独用真实的多 span 文字层结构
(`O(N`、`2`、`)` 分属不同 div 这种)验证过 8 个用例。**没验证的**:真实 PDF 文字层上
end-to-end 跑一遍——AI 真的写一条进文件、Obsidian 重载后是否真的定位到对的段落,
这个只能用户自己试。

### v0.9.0 —— 配对不再需要手动打开两份(用户追问逼出来的改进)

用户问了两件事:"为什么要指纹,直接按名字加载不行吗"、"打开一个能不能自动读另一个,
非要我手动吗"。第一个已经有实测数据回答过(86 个 PDF 里名字匹配 24 对、22 对是假的,
指纹不是性能考虑,是防止把不相关文件的批注悄悄合并到一起)。第二个是真问题——之前
指纹只能来自"已经打开的 viewer",所以配对必须两边都手动开过一次。

**发现姐妹插件已经踩过这条路,而且是验证过的:**
`everything-bilink/src/render-image.ts` 已经在用 `window.pdfjsLib`(Obsidian 加载
PDF 后挂在全局上的同一个 pdf.js 实例,不是另外打包一份)做无 UI 的 PDF 解析,给批注
渲染裁剪预览图用。同样的手法直接搬过来做无 UI 指纹采集:`getPage(1).view` 拿页面尺寸、
`doc.numPages` 拿页数、`getTextContent()`(不需要任何 DOM/文字层渲染,纯 pdf.js 内部
API)采样前 3 页算中文占比——**不开任何标签页,没有视觉闪烁**。

**新文件 `headless-fingerprint.ts`。** 一个真实限制说清楚:`window.pdfjsLib` 是懒加载
的,本次会话里得先打开过至少一个 PDF 才会存在——但这恰好不是问题,因为这个函数只在
`tryAutoPair` 里被调用,而 `tryAutoPair` 只在"某个 PDF 已经被打开并采完指纹"之后才会跑,
这本身就意味着 `window.pdfjsLib` 已经加载好了。

**`tryAutoPair` 改动**:候选没有指纹时,不再是"跳过、等用户哪天手动打开",而是立刻
后台拉一次。`headlessInFlight` 从 `Set<string>` 改成 `Map<string, Promise<void>>`
——不只是去重(同一个候选被两处同时触发不会拉两次),还让 `switchToCounterpart`
能真的**等**这个 Promise(最多 4 秒),而不是"检查一下还在跑就直接报错、逼用户手动
再点一次"。`explainNoCounterpart` 的提示文案也跟着改了:"候选还没打开过"变成
"正在后台读取,马上再试" / "读取失败了(可能不是有效 PDF)"。

**没有测试覆盖**——这个功能的核心依赖(`window.pdfjsLib` 是否存在、行为是否和文档
里描述的一致)是浏览器运行时状态,在 Node 里没法有意义地模拟,只能靠
`render-image.ts` 已经在生产环境用了这个模式这一点做底气,而不是新的自证。

### epub(讨论,未实现)

用户确认了 epub 的批注模型:选中文字 → 加批注(没有页码/PDF 坐标概念,只有 DOM 里的
文字位置);点击高亮 → 显示批注;侧边栏同一套逻辑。这意味着 PDF 现在用的
`anchor: PdfRect` 完全不适用于 epub,得换成文字引用锚点(`{quote, prefix, suffix}`,
姐妹项目 `~/Documents/project/margin-notes-web/src/anchor.ts` 已经有实现可以照抄)。
结构上可行——把"锚点→屏幕坐标"抽成接口,PDF/epub 各一个实现,便利贴/轨道/避让/渲染
全部复用——但这是不小的重构,还没动手,等 PDF 这版彻底跑顺再说。

### 还没做 / 下一步

1. 上面"完全没验证"整节的真实 UI 验证
2. **双链不进反链系统**——批注文字在 JSON 里,Obsidian 的 metadataCache 不解析它,
   所以 `[[某某]]` 只是单向可点,目标笔记看不到反链、图谱也不连线。已和用户讨论过
   改存 Markdown(一个 PDF 配一个批注 md,机器字段塞 HTML 注释)来彻底解决,
   **这个决定还悬着,没做**
3. epub 支持——见上面的讨论小节,数据模型需要一次不小的重构才能两边共用
4. AI 写批注:当前 schema 对 AI 只读友好、写不友好(锚点是坐标,AI 推不出来)。
   讨论过的方向是加 `quote`/`prefix`/`suffix` 文字锚点(姐妹项目
   `~/Documents/project/margin-notes-web/src/anchor.ts` 已有同款实现可抄),
   页码作为更粗的精度档兜底。**也还没做**
5. `revealAnnotation` 用固定 350ms 等 pdf.js 渲染完目标页,不稳;应该改成监听一次
   `pagerendered`
6. 各页尺寸/旋转不一致的 PDF,轨道会左右跳(大多数 PDF 每页一致,真遇到再修)

### v0.10.0 —— 轨道设置拆分左右 + 悬浮高亮原文位置

用户反馈四件事,顺着查代码发现前三件其实是同一个根因:`railWidth`/`railGap` 从
一开始就是**一对全局共享设置**,左右两条轨道读的是同一个数字——设置面板的说明文字
写的是"改一条就是改这一侧的全部",代码却没照着做。于是调左边宽度,右边跟着变;
调大左边的 gap,右边也跟着空出一截"留白";用户把这些症状描述成"很奇怪"完全合理。

**实测过而非猜测**:跑了一遍插件实际的名字匹配逻辑对着 vault 里真实的 100 个 PDF,
25 个候选里 23 个是假的(`hopfield-layers_gh` 示例仓库里同名图注 PDF、`.trash`/
`zz_old` 里重复的 matplotlib 图标文件、甚至 70_research 论文堆里两份大小写不同的
同一篇英文论文)——这是回答"为什么要指纹"那个追问时顺手做的验证,细节见上面
v0.9.0 之前的讨论,这里不重复。

**改动**:

- `annotation-settings.ts`:`railWidth`/`railGap` 拆成 `railWidthLeft` /
  `railWidthRight` / `railGapLeft` / `railGapRight`,各自独立。旧的单一值会在
  加载时原样铺到左右两侧(不会让老用户的轨道突然跳一下)。
- `annotation-layer.ts`:新增 `railWidthPt(settings, side)` / `railGapPt(settings, side)`
  两个按侧取值的小helper,`layout()`、`attachRailResize()` 全部改用它们;
  `createBox`/`attachResize` 不再需要把 `railWidthPt` 当参数一路传下去了。
- `annotation-settings-tab.ts`:两个滑块变成"左侧轨道"/"右侧轨道"两组四个滑块。

拖拽两条边的几何公式本身(内边动宽度、外边动 gap,外边固定不动)重新演算过一遍,
数学上是对称、成立的——没找到左右不对称的独立 bug。如果拆开全局设置之后
"左边轨道右边的边还是拖不动"依然复现,那是另一个问题,需要用户下次描述得更具体
(比如是完全没反应,还是反应了但方向不对)才能继续查,这次没法在没有实时 UI 的
情况下确认。

**悬浮高亮原文位置**(用户的第一个诉求:"完全不知道原文在哪里")。之前只有点击
列表面板的跳转按钮才会画高亮框(`reveal()`,一次性、带 1.6s 淡出动画、还会滚动
视图)。现在拆出一对新方法 `beginHoverHighlight`/`endHoverHighlight`,复用同一套
锚点几何计算(重构进 `drawAnchorMark` 私有方法),但**不滚动、不带定时消失**——
鼠标移进移出即时开关,画一个新 CSS 类 `.margin-notes-pdf-anchor-hover`(纯 opacity,
没有 keyframe 动画)。两处接入:

- PDF 页面上的批注框本身(`createBox` 里 `mouseenter`/`mouseleave`)——对轨道里的
  固定批注最有用,因为它离锚点原文本来就有一段距离。
- 列表面板每一行(`annotation-list-view.ts` 的 `renderRow`)——通过控制器新增的
  `peekAnnotation(ann)`/`clearPeek()`,内部找当前打开的 PDF 视图状态,静默失败
  (锚点所在页没渲染在屏幕上就什么都不做,不会强行跳转)。

`npx tsc -noEmit -skipLibCheck` 和 `npm run build` 都过。**没有测试覆盖**——
悬浮高亮的位置计算复用了 `reveal()` 已经在用的公式,几何上应该一致,但没有实时
UI 验证过实际观感(比如淡入是否够明显、轨道批注悬浮时高亮框会不会跳到屏幕外)。

### v0.10.1 —— 轨道批注缩放后顺序/间距变乱(offsetY 单位错了)

用户追问上一版:"为什么这些批注都不是绝对位置,缩放之后都会变?" 澄清后是"轨道里的
相对顺序/间距变乱了",不是泛泛的好奇——这是个真 bug,找到了。

**根因**:手动上下拖一条固定批注(在轨道里调整顺序)时写入的 `offsetY`,一直存的是
**拖拽时的原始屏幕 px**,不是 PDF 点。这层里其他所有几何量——`anchor`、
`railWidth(Left/Right)`、`railGap(Left/Right)`——都特意存成 PDF 点、渲染时再按
`box.zoom` 换算成 px,这样才能保证跟页面一起缩放。唯独 `offsetY` 漏了这一步:
存的时候没除以 zoom,读的时候(`anchorTop()`)也没乘回来。结果就是——你在 100%
缩放时手动把某条批注往下拖了 50px 好跟另一条错开,这个 50px 会被冻结;换一个缩放
级别,批注锚点本身的位置(以及同轨道里其他批注的位置)都按新 zoom 重新计算了,
唯独这条被手动调过的批注的偏移量还是死的 50px——相对位置自然就乱了,轻则间距不对,
重则排序整个翻过来。**没手动拖动过的批注不受影响**,因为它们的位置完全由 `anchor`
算出来,本来就是按点存的。

同一次顺手把 `MIN_GAP`(轨道里两条批注之间的最小间距,原来固定 8px)也改成按 zoom
缩放——原因一样:8px 在放大很多时看起来快贴一起了,缩得很小时又显得空得不合理。

**改动**:`annotation-layer.ts` 的 `anchorTop()` 读 `offsetY` 时乘 `box.zoom`;
`attachDrag` 的 `onUp` 写入时除以 `box.zoom`;`Rail` 接口加一个 `zoom` 字段,
`layout()` 里算碰撞间距时 `MIN_GAP * zoom`。`annotation-types.ts` 里 `offsetY`
字段的文档注释同步改了。

**迁移是无感的,不需要写迁移代码**:旧数据里存的 `offsetY` 数值,在用户拖拽时的
那个缩放比例下,点和 px 是同一个数(zoom=1 时二者恒等)。所以老用户重载插件后,
只要还停留在 100% 缩放,画面不会有任何变化;换到别的缩放级别,这条批注才会开始
按点正确缩放,而不是像以前那样冻结不动。

`npx tsc -noEmit -skipLibCheck`、`npm run build` 都过。**没有测试覆盖**——这次
是纯静态代码审查加数学推导定位到的问题(检查了 `resolveCollisions` 本身的排序逻辑
没问题,问题出在喂给它的 `top` 值上),没有在真实 Obsidian 里缩放验证过修复后的实际
效果,需要用户下次缩放试一次已经手动调整过顺序的轨道批注。

### v0.11.0 —— 三个报错一次修完(悬浮不高亮 / 右侧空白 / 缩放后变形)

用户一次报了三个问题,查下来是三个独立的根因,其中第一个是**从用户真实数据里直接
看出来的**,不是猜的。

**1. 悬浮不高亮原文 —— 跨语言配对把锚点污染了。**
读用户的 `annotations.json` 发现:DMHN 那 8 条 AI 写的批注里,**6 条的 anchor 都是
`[20, 732, 220, 772]`**——这正是 `resolveAnchorIfNeeded` 里那个兜底矩形
(`x0+20, y1-60, x0+220, y1-20`,612×792 页面上算出来就是这个数)。

原因链:这些批注的 `quote` 是**中文**(写给译文的),但它们存在**英文原文**的桶里
(配对的 canonical key 是英文那份)。所以打开英文 PDF 时,拿中文 quote 去搜英文文字层
必然搜不到 → 走兜底 → **兜底矩形被写进了 `ann.anchor`** → 此后 `isUnresolvedAnchor`
永远返回 false → 再也不会重试,而且任何一次 upsert(拖一下、编辑一下)就把这个垃圾
坐标持久化到磁盘了。之前的注释里写着"兜底不会持久化",这句话是错的——它没有直接
调 upsert,但它污染了 store 持有的活对象,后续任何一次保存都会带上。

改法:`resolveAnchorIfNeeded` 改名 `effectiveAnchor`,**返回**兜底矩形而不是写回
`ann.anchor`,`Placed` 上加一个 `rect` 字段承载它。这样未解析的记录永远保持未解析,
每次 rebuild 都会重试——**用户只要打开一次中文译文,quote 就能匹配上并持久化真实
坐标;又因为译文是保版式的,这个坐标对英文原文同样成立**,两边就都好了。
`drawAnchorMark`(悬浮/点击高亮)也改了:未解析时现场做一次 quote 查找,**查不到就
返回 null 不画**——宁可不高亮,也不要自信地高亮到错误的位置,毕竟这个功能存在的
唯一意义就是回答"原文在哪"。

顺手把用户数据里那 6 条污染的 anchor 重置成 `[0,0,0,0]`(只重置**同时**满足"精确
等于兜底矩形"和"还带着 quote"两个条件的,手动摆过的一律不动),备份在 scratchpad。

**2. 右边空白很多 —— 用户自己猜对了,就是右上角那两个按钮。**
`.margin-notes-pdf-body` 上有一条 `padding-right: 34px`,给悬浮才出现的工具栏
(拖动手柄 + ⋯)预留位置。但 padding 是**每一行都留**、而且**永远留**,窄框上这就是
文字栏的一大半。改成 `::before` 的右浮动占位块:只有工具栏真正压住的那一两行会缩进,
下面的行用满宽。(注意 `:empty::before` 占位符规则和它是同一个伪元素,得显式
`float: none` 覆盖回来,否则空批注的提示文字会变成一条 34px 宽的竖条。)

**3. 缩放后轨道批注变得极长 —— 只缩放了两个属性,其余 chrome 全是固定 px。**
之前的做法是把 zoom 直接乘进两个值:`width = railWidthPt * zoom`、
`fontSize = fontSize * zoom`。但盒子自己的 `padding: 5px 7px`、边框 2px、
工具栏图标 15px、上面那 34px 预留——**全是不缩放的固定 px**。缩小到 30% 时,220pt
的轨道只有 66px 宽,其中 48px 是不缩放的 chrome,**真正能放字的只剩约 18px**,一行
两三个字,高度又是自适应的,于是盒子被撑得奇高。用户描述的"宽度变小导致一行文字很少,
然后高度自适应所以很高"完全对得上。

改成**整体 `transform: scale(zoom)`**:元素按自然尺寸(点为单位)布局,再整块缩放。
padding、边框、字号、工具栏一起等比缩放,永远保持比例;而且**换行是在与 zoom 无关的
宽度上算的,所以同一条批注在任何缩放级别下断行完全一致,高度根本不再变化**。
连带改动:`offsetWidth/offsetHeight` 现在拿到的是未缩放值(transform 不影响它们),
所以碰撞检测的高度要 `* zoom` 还原,拖拽 resize 的光标位移要 `/ zoom` 换算成点——
好处是保存时不用再做任何单位换算了。自由便签的 `freeW/freeH` 仍存页面百分比,
在 `placeFree` 里除以 zoom 转成未缩放尺寸。

**验证**:`tsc` + `build` 通过;另外在 scratchpad 写了一个纯算术的
`zoomcheck.mjs`,把 `railLeft`/`scaleBox`/`attachRailResize.solve` 的点单位运算
镜像出来,断言了(a)屏幕尺寸与 zoom 严格成正比、左右轨道都对,(b)同样的"点"位移
在 0.3/1/2.5 三种缩放下拖拽,存下来的宽度/间距完全相同,(c)拖内侧边时外侧边确实
不动,(d)宽度下限仍然生效——全部通过。**没验证的**:transform 缩放后的文字清晰度
(Chrome 对静态 transform 会按最终尺寸重新栅格化,理论上不糊,但要用户自己看一眼),
以及 contentEditable 在缩放状态下的光标表现。

### v0.12.0 —— 中文批注根本锚不上(quote 匹配对 CJK 是坏的)+ 留白改法 + 手动改高亮

**1. 中文没法悬浮高亮 —— quote 匹配逻辑对中文从一开始就是错的。**
v0.11.0 修好了"兜底坐标污染锚点",但用户反馈中文**还是**不高亮。查下来是更底层的
问题:`quote-anchor.ts` 把文字层的多个 span 拼成一整串时,**在每两个 span 之间插了
一个空格**。这对英文是对的(span 边界通常就是词边界,空格本来就该有),对中文是
致命的——中文没有词间空格,所以:

```
文字层拼出来: "皮层和海马回路中的 神经群体活动可被情境灵活重组"
批注里的引文: "皮层和海马回路中的神经群体活动可被情境灵活重组"
indexOf → -1
```

用户那 8 条批注里唯一锚上的那条,正好是整句都落在同一个 span 里的,所以躲过了这个 bug。

改法:匹配键改成"小写 + **删掉所有空白**"(不是原来的"折叠成单个空格"),两边都做
同样的变换,所以对英文无害("the quick" 和 "thequick" 压成同一个键),对中文则彻底
绕开了分隔符问题——**一条代码路径,不是中英分叉**。为了还能把匹配位置映射回
`computeSelectionRects` 需要的 [div 索引, 偏移],内部维护两套坐标:`raw` 是不加任何
分隔符的纯拼接(索引可直接映射回 div 内偏移),`keep[]` 把压缩串的位置映射回 raw。

scratchpad 里 `qa.mjs` 用 8 个用例验证过,包括用户数据里的真实中文句子跨 span、
中文两端都在 span 中间、文字层里夹杂零散空格(译文 PDF 常见)、英文跨 span、
`O(N` + `2` + `)` 这种公式拆分、查无此句、太短不给匹配、跨三个 span——全过,而且
断言的是"反向抽取出来的文本等于预期",不只是"匹配到了"。

**2. 右上角留白:改成把第一行往下压,不再横向让位。**
用户提议"第一行往下移一点,绝对不会碰到右上角"——比之前的浮动缩进好,采纳。
`padding-right: 34px`(每行都留)→ 首行右浮动占位(只缩进头几行)→ 现在是
`padding-top: 15px`(整体下移一次)。宽度是窄边注最稀缺的东西,用一次性的 15px 高度
换**每一行的完整宽度**更划算,而且无论批注多窄都不可能撞上右上角。plain 样式不留
(`padding-top: 0`),它本来就不该有 chrome。

**3. 新增:菜单里手动重设高亮位置。**
之前没有任何 UI 能修一条锚错的批注——引文匹配不上、或者写给了配对里的另一种语言,
就只能去改 JSON。现在批注右键菜单和列表面板行上都加了「重新指定高亮位置」:
有选中文字就直接用选区,没有就武装一次性拖框(复用 `addNote` 已有的
`rectSelect.armed` + `attachRectSelectListener` 机制,新增 `pendingReanchor` 分支)。
落定时**同时清掉 `quote`**——人已经指明了真实位置,这个矩形就是真相,留着旧引文会让
以后某次重新解析把用户的手动修正悄悄覆盖掉。

**没验证的**:三项都没在真实 Obsidian 里跑过。第 1 项的字符串逻辑有上面的离线用例
兜底,但"真实 pdf.js 文字层的 `textContent` 到底长什么样"仍是假设(用例里模拟的
span 切分方式来自之前 `everything-bilink` 的经验,不是从这份 PDF dump 出来的);
第 3 项的拖框交互完全没测。

### v0.12.1 —— 自由便签滑不到:左边不可达 + 可滚动范围只按"打算放哪"算

用户:"自由便签都被遮住了看不到,好像只以轨道最边界为边界,应该以最边边的那个,
不然我左右滑动都看不到"。两个独立缺陷:

**1. 左边根本不可达(真 bug)。** 滚动容器的坐标不能为负——放在 x < 0 的元素不是
"划出去了",是**永久看不到、也滚不到**。左侧轨道一直有 `Math.max(0, ...)` 兜着,
**自由便签完全没有这个钳制**。所以一条被拖到页面左侧空白里的便签,只要空白后来变窄
(放大、面板收窄、页面变宽),就会滑到负坐标里彻底消失。加了 `reachableX()`,自由
便签和收起的点都过一遍。被钳住的便签会压在页面左边缘上而不是待在旁边——这是刻意
选的:压着至少还能看见、还能拖走,消失了就什么都做不了。

注意**只钳制渲染位置,不钳制存储的 `freeX`**。存的还是用户拖到的真实值,所以以后
空白变宽了,便签会自己回到原本摆的地方,而不是被永久拍平到边缘。

**2. 可滚动范围按"打算放哪"算,而不是"实际在哪"。** `layer.style.width` 之前完全
来自 `maxRight` 这个累加值——每个分支自己往里塞它**打算**放的右边界。任何一个分支
算错(或者像 transform 改造后那样单位换了一轮),可滚动区域就会被悄悄截短,而截短的
后果正是"越过截断点的便签全部滚不到"。改成同时用 `getBoundingClientRect()` 实测所有
已放置元素的真实右边界(transform 之后的),取两者的大者。实测值和屏幕上看到的东西
不可能不一致,所以边界现在真的是"最边边的那个",不管它是轨道还是便签。

**没验证的**:两项都没在真实 Obsidian 里跑过。第 2 项是防御性改动——我没能证明
`maxRight` 当前确实算错了(它的算式看着是对的),只是把这个类别的错误整个消除掉,
因为它的失败模式对用户来说就是"东西凭空消失"。如果重载后还是滑不到,那问题更可能
出在 pdf.js 滚动容器本身(比如 `overflow-x` 被限制),届时需要用户从控制台报一下
`scrollWidth`/`clientWidth` 和滚动容器的 computed style。

### v0.12.2 —— 解除固定后便签跑很远

`togglePin` 解除固定时只把 `pinned` 设成 false,**不写 `freeX/freeY`**,于是渲染时
落到 `freeXPct/freeYPct` 的**默认值**上——而那两个默认值是从 `anchor` 算出来的。
对一条待在轨道里的批注来说,那是完全不同的位置:

- 横向:从轨道车道直接瞬移回原文旁边(轨道离页面多远,就跳多远)
- 纵向:同时丢掉手动 `offsetY` 微调**和**碰撞避让给它的位移

之前那段注释说"默认值正好把它放在锚点旁边,正是刚离开轨道的批注该在的位置"——这个
判断是错的。「解除固定」的语义是"不再排在车道里",不是"跳到别处去"。

改成解除固定时读出**当前渲染出来的盒子**(`el.style.left/top` + `offsetWidth`)并写回
`freeX/freeY/freeW`,位置和宽度都原地冻结。`el.style.top` 是碰撞避让写完之后的值,
所以拿到的是真实最终位置而不是碰撞前的锚点位置。`offsetY` 归零(它已经被折算进
`freeY` 了,留着会在以后重新固定时被重复叠加一次)。拿不到元素或页面几何时退回原来
的行为。

scratchpad `unpin.mjs` 验证了"解除固定写入 → 下次渲染读出"这一对变换在 0.3/1/2.5
三种缩放下是**恒等**的(位置、屏幕宽度、未缩放宽度四项全等),另外测了左轨道被
`reachableX` 钳到 0 的情况也能原样往返。**没在真实 Obsidian 里点过。**

### v0.13.0 —— 放大后左侧批注全部挤在页面边缘(左右不对称的根因)

用户:"放大之后左边的批注全部抵住左边往右边挤,完全脱离了原来的位置,右边是正常的"。

**根因是左右两侧本来就不对称,而且之前一直用钳制在掩盖它。**
右侧不需要额外空间:越过页面右边缘的批注只要把 layer 的宽度撑大,滚动容器就能滚过去。
左侧没有这个自由——**滚动偏移不能为负**,所以 `railLeft` 在左侧一直有个
`Math.max(0, ...)` 兜底。平时页面两侧有留白,轨道塞得下,钳制不触发;而**放大恰恰是
在消灭这块留白**(页面越放越大,直到撑满视口,左侧留白归零)。留白一旦小于轨道需要的
宽度,每一条左侧批注算出来的 x 都是负数,于是**全部被钳到同一个 0**,叠在页面边缘,
相对位置彻底丢失。这就是"右边正常、左边塌掉"的全部原因。

钳制是在治标。真正的修法是**让这块空间真的存在**:根据左轨道自己的宽度 + 间距 ×
当前缩放,给滚动容器加一个 `padding-left`,把页面整体往右推,于是永远有真实的留白
可站,钳制再也不会触发。

几个关键点:
- **必须在测量任何东西之前应用**,因为它会移动页面,之后读到的所有 rect 才是对的。
- **只由轨道设置和缩放推导,绝不能用测量到的页面位置**——padding 会改变页面位置,
  把它再喂回去会自激振荡。
- 写入是幂等的(值没变就不碰 DOM),避免反复触发 pdf.js 的重排。
- 这个 padding 加在 **pdf.js 自己的元素**上,不是我们自己的节点,所以
  `destroy()` 和切换 viewer 时都必须显式还回去,否则会给一条已经不存在的轨道
  长期占着位置。
- 绝对定位的包含块是**padding box**(不含 padding 区域),所以加 padding-left
  不会移动 layer 的坐标原点,`pageBox` 的测量和 layer 里子元素的定位仍然对齐。

scratchpad `gutter.mjs` 在 5 种缩放 × 4 组轨道参数(含极端的 500pt 宽、300pt 间距、
以及负间距)下断言钳制**从不触发**、且钳制前后的值一致、还留有 OUTER_MARGIN 的余量;
另外加了一个对照用例复现旧行为(无 padding、zoom=4 时确实算出 0),确认这个测试真的
在考察它声称的那个 bug。**没在真实 Obsidian 里放大验证过。**

### v0.14.0 —— 高亮显示方式(4 种)+ 高亮透明度 + 颜色对应 + 工具栏扩充

用户一次提了 6 条,全部实现。

**1. 四种高亮显示方式**,设置里下拉选,也有命令「[PDF] 切换高亮显示方式」:
- `note` 只有悬浮批注时才高亮原文(旧行为,仍是默认)
- `both` 双向:悬浮批注高亮原文,**悬浮原文也高亮对应批注**
- `always` 一直显示所有高亮
- `line` 用细线从批注指向原文,不涂色块

命令用 `Menu` + `setChecked` 而不是"循环切换":四个模式循环的话,你得一路点过去才知道
当前在哪个;菜单里当前项打勾,一眼就看得到。

**`both` 的反向命中检测特意不用真实元素。** 在原文上盖一层
`pointer-events: auto` 的热区最省事,但那会**吞掉文字选择**——而选中文字正是新建批注
的入口,等于为了一个显示功能废掉核心交互。所以改成在滚动容器上挂一个 `mousemove`,
拿光标坐标去和记录下来的锚点矩形做命中测试。另外加了一道防抖:光标在批注上时直接
返回,否则批注自己的 `mouseenter` 刚点亮高亮,下一个 `mousemove` 发现光标底下没有
锚点就立刻把它清掉,两边打架。

**2. 高亮透明度**独立设置项(5-100%),走 CSS 变量
`--margin-notes-pdf-highlight-opacity`,只影响原文上的色块和指示线,不动批注自己的
不透明度。

**3. 高亮颜色跟随批注颜色**(用户建议):新增 `colorOf(ann)` —— 单条批注的 `color`
优先,否则按固定/自由取全局默认色。悬浮高亮、常驻色块、指示线全部走它,所以同一页
多条批注时一眼能对上谁是谁。

**4. 工具栏扩充**(用户观察到上面已经留了空):正因为 v0.12.0 把正文改成从工具栏
下方开始,图标不再占用文字宽度,所以最常用的几个可以放回右上角了 ——
字号减 / 字号加 / 颜色 / 有无边框 / ⋯。颜色那个按钮直接染成批注自己的颜色,兼作色块
指示。菜单里这些项全部保留,没有只在工具栏才能做的操作。

**验证**:`leader.mjs` 验证指示线几何 —— 4 种相对位置(批注在右/在左/在上/正好齐平)
下,断言旋转后线段的远端**精确落在锚点上**,且起点取的是批注朝向原文那一侧的边、
终点取的是原文靠近批注那一侧的边(而不是各自的远边)。`tsc` + `build` 通过。
**四种模式都没在真实 Obsidian 里看过**,尤其 `both` 的 mousemove 命中测试和
`line` 的实际观感。

### v0.15.0 —— 用户一次提的 7 条

**1. 常亮色块和悬浮高亮外观不一致。** 真原因:`.margin-notes-pdf-anchor-hover`
在 styles.css 里**存在两个相隔 200 行的定义块**(一个旧的写死
`background: var(--interactive-accent); opacity: 0.32`,一个 v0.14.0 追加的用
批注颜色 + 设置里的透明度)。靠后者覆盖前者"碰巧"能工作,但这正是两份声明必然逐渐
分叉的典型情形。改成常亮色块和悬浮色块**共用同一条规则**——它们本来就是同一个东西,
只是显示时机不同。

**2. 细线可以直接拖动改位置。** 把 `attachDrag` 里的拖拽逻辑抽成
`beginNoteDrag(ev, el, pdfPath, ann, pageView)`,手柄和指示线共用。线只有 1px,
靠 `::before` 撑出上下各 5px 的命中区。**代价说清楚**:线现在是
`pointer-events: auto`,所以它经过的那一条窄带会挡住 PDF 的文字选择。线大部分走在
页边空白上,只有末端压到正文,判断是可接受的;如果实际用起来碍事,得改成按住修饰键
才激活。

**3. 右键高亮区域直接改批注。** 在滚动容器上加 `contextmenu`,命中锚点矩形就弹出
该批注的菜单。**只在真的命中时才 preventDefault**,右键点别处仍然是 Obsidian 自己
的菜单。锚点矩形现在**在所有模式下都记录**(不只是需要反向悬浮的模式),所以哪怕在
「单向」模式下右键原文也能改。

**4. 命令改用 Obsidian 的建议器样式**(新文件 `highlight-mode-picker.ts`,
`FuzzySuggestModal`),不再是飘在面板边上的 `Menu`。当前项前面加 ✓,而且这个勾是
**可搜索文本的一部分**,过滤之后仍然看得出当前在哪个模式。

**5. 取色盘就近弹出。** `pickColor` 造的那个临时 `<input type=color>` 之前没设坐标,
系统取色面板就跟着它出现在窗口角落。现在把 input 定位到点击位置,面板就在批注旁边。

**6. 拖动手柄移到真正的右上角。** 原来是 `prepend` 到工具栏(最左),现在 `append`
(右对齐工具栏的最后一格 = 角落本身)。顺带给工具栏加了
`max-width: calc(100% - 6px)` + `flex-wrap`:批注是 `overflow: hidden`,v0.14.0 加到
6 个图标后,窄批注上最左边的图标会被**静默裁掉**,换行至少保证每个图标都还够得着。

**7. 常亮模式悬浮不点亮对应批注。** 用户说得对,只有一半的链接等于没有链接——
看到页面上一条色块却不知道它属于哪条批注,和只有正向一样没用。反向悬浮**不再是
`both` 模式独有**,改成除「单向」外全部开启,由 `highlightsBothWays(mode)` 统一判定。
四个模式按用户给的名字改成:**单向 / 双向 / 箭头 / 常亮**。

`tsc` + `build` 通过。**七条都没在真实 Obsidian 里验证过**,其中第 2 条的文字选择
干扰、第 6 条工具栏换行后的观感,是我自己最不确定的两处。

### v0.16.0 —— 常亮偏深 / 箭头拖的是哪一端 / 箭头慢半拍 + 重写批注

**1. 「常亮」比「悬浮」深得多。** 不是颜色配错,是**叠了两层**:常亮模式下锚点上已经
有一块常驻色块,悬浮时 `beginHoverHighlight` 又**在它上面再画一块**。两层 30%
合成约 51%,所以一悬浮就明显变深。而且 v0.15.0 刚把反向悬浮在常亮模式下也打开了,
等于常态就在叠。改法:`bands` 按 id 记下常驻色块,悬浮时**给它加个
`is-active` 类提亮**,而不是新建元素;只有没有常驻色块的模式(单向/双向/箭头)才
画临时的那一块。

**2. 箭头拖的应该是「选中位置」那一端,不是批注。** 用户指出 v0.15.0 做反了:批注
本来就有拖动手柄,真正没有手柄的是**锚点**。改成在箭头的**文字端**放一个小圆点
(knob),拖它平移整个锚点矩形——高亮落错行时可以直接拽到对的那一行;落定时按
`zoom` 换算成 PDF 点,并且 **Y 轴取反**(PDF 的 y 向上,屏幕向下)。同时清掉
`quote`,理由和「重新指定高亮位置」一致:人已经指定了,后续重新解析不能覆盖它。
想**重选一段文字**仍然走选中 + 「重新指定高亮位置」——拖动是微调,选择是重选。
顺带把线本身改回 `pointer-events: none`(v0.15.0 让整条线可点,会挡住 PDF 的文字
选择,而选中文字是新建批注的入口),现在只有那个小圆点接收指针。

**3. 拖批注时箭头慢半拍。** 因为线只在防抖过的 rebuild 里重画。把定位逻辑抽成
`positionLeader(parts)`,`beginNoteDrag` 的每次 `pointermove` 都调一次
`refreshLeader(ann.id)`,线就跟着走了。

**4. 重写了 DMHN 的批注。** 之前那 8 条被跨语言配对搞坏了:6 条卡在兜底坐标
`[20,732,220,772]`,而且 pinned/side 的组合也被历次改动弄乱(左侧的变成了自由便签)。
清掉重写成 **14 条,页 1-4,全部 pinned**,左侧 7 条是读者提问、右侧 7 条是模拟作者
内心 OS,只带 `quote` 不带 `anchor`(交给插件在译文上解析)。

**这次写之前先验证了引文真的存在**:`write_notes.py` 用 PyMuPDF 抽出译文每页文字,
按插件同样的规则(小写 + 删掉全部空白)做包含判断,**任何一条对不上就直接拒绝写入**
而不是写完等用户发现锚不上。14 条全部通过。**残余风险**:PyMuPDF 的 `get_text()`
和 pdf.js 文字层的 `textContent` 不保证逐字相同(连字、软换行、图注里的公式占位),
所以"能在 PyMuPDF 里找到"不等于"一定能在 pdf.js 里找到"——但这比之前完全不验证
强得多。

`tsc` + `build` 通过。**前三条都没在真实 Obsidian 里看过。**

### v0.17.0 —— 撤销/重做 + 箭头端点改为回弹

**1. 支持系统撤销快捷键。** store 里加了快照式的撤销/重做历史(上限 100 步)。
用快照而不是反向操作:这里的修改种类少、数据小(一整个库的批注也就几十 KB),
而快照**不可能和实际数据脱节**;反向操作则要求每种未来新增的修改都写对逆操作,
漏一个就会污染它之后的全部历史。`pairs`/`fingerprints` **故意不纳入**——它们描述的是
文件之间的关系而非用户写的内容,删了一条批注按 Cmd+Z 结果把两份文档解除配对,
那是惊吓不是撤销。

命令绑在 `Mod+Z` / `Mod+Shift+Z`,但 `checkCallback` **只在 PDF 视图有焦点且确实有东西
可撤销时才返回 true**——返回 false 时按键会留给别的处理者,所以编辑器里的 Cmd+Z
一切照旧。

**写测试时抓到一个真 bug(不是假想的)**:`mutate()` 是**原地**修改批注对象的,而那个
对象**就是 store 列表里的同一个引用**。等 `upsert` 里 `pushHistory()` 拍快照时,
修改**早就已经应用上去了**——所以"修改前"的快照拍到的是修改后的值,**撤销任何一次
文字/颜色/位置修改都会是静默无效**,只有新增和删除能撤销。修法是让
`forPage`/`forFile` **返回副本**,`upsert` 也存副本:调用方改的是副本,store 自己那份
在 upsert 之前保持原样,快照才真的是"之前"。另外给 `upsert` 加了
`recordHistory` 参数,自动的 quote→anchor 解析传 `false`,否则撤销栈会被没人做过的
操作塞满。

**2. 箭头端点改成回弹 + 提示。** 用户指出 v0.16.0 的方向不对:拖端点**不应该**改变
选区。高亮记录的是"这段内容实际在文档的哪里",让一次误拖悄悄改写它,等于在没有任何
提示的情况下丢掉真实位置——而这个圆点就浮在页面上,很容易蹭到。改成橡皮筋:按住时
线跟着走,松手**弹回原位,锚点一个字节都不写**;拖出 24px 以上还会提示真正改高亮的
方法(选中文字 → 菜单里「重新指定高亮位置」)。重新锚定保持为显式操作。

`undo.mjs` 覆盖:多步撤销/重做、重做到头是 no-op、**新修改清空重做栈**、删除的往返、
**对已有批注做修改后撤销能恢复旧文字**(就是上面那个 bug 的回归用例)、
`recordHistory:false` 不产生历史、历史长度封顶。全过。
**两条都没在真实 Obsidian 里按过键。**

### v0.18.0 —— 箭头端点在选区内可动 / 两种悬浮统一 / 预设色替代取色盘

**1. 箭头端点可以拖,但**只能在高亮范围内**。** v0.17.0 做成了完全回弹,方向也不对。
用户要的是:能调,只是不能超出选区。新增 `leaderAt?: {x,y}`(锚点框内的 0–1 比例,
持久化),默认是靠近批注那条边的中点。拖动时把指针位置换算成比例并**钳到 [0,1]**,
所以点永远落在高亮上;指针跑出选区 24px 以上才提示怎么真正改选区。

这个区分是有道理的:**移动附着点是表现层的**(箭头从更顺眼的位置出发),误操作零代价;
**移动选区是内容**——它定义了这条批注说的是文档的哪一段,一次误拖悄悄改写它,等于
没有任何提示地丢掉真实位置。所以前者自由,后者必须显式走「重新指定高亮位置」。

**2. 悬浮批注和悬浮原文现在长得一样。** 之前悬浮原文会给批注加 `is-linked`
(带描边),悬浮批注本身却只有透明度变化——同一种关系两种外观。现在两边都加
`is-linked`。描边颜色改用**批注自己颜色的加深版**(新增 `darken()`,在 JS 里算,
不依赖 `color-mix` 的渲染器支持),不再用主题强调色:强调色描边在每条批注上长得
一模一样,**根本说明不了是哪一条亮了**,而且看起来像"被应用选中"而不是这条批注的身份。

**3. 取色盘换成设置里的预设色。** 系统取色面板是靠一个隐形 `<input type=color>` 触发
的,面板只会开在那个 input 旁边,怎么摆都容易跑到角落。删掉整条路径,改成
`swatch-picker.ts`:一个自己画的小浮层,开在点击处(并且会按自身尺寸避开窗口边缘),
列出设置里配的预设色 + 一个「默认颜色」。设置里用文本框填 `#RRGGBB`(空格或逗号
分隔)并带实时色块预览;**解析结果为空时拒绝保存**——存进一个空调色板会让取色浮层
没有任何颜色可选,而且除了手改 JSON 没有别的退路。

固定预设色的另一个好处:一篇文档里的批注颜色会成体系,而不是每次随手挑一个。

`clamp.mjs` 验证端点钳制:中心映射、四个方向拖出去都钳回边界、**钳后的点一定落在
矩形内**、小幅越界不打扰、拖动过程中一度远离即使最后拉回也仍然提示、
0–1 全程往返都在框内、零高度选区不除零。全过。
**三条都没在真实 Obsidian 里点过。**

### v0.19.0 —— 撤销吃掉了文本撤销(回归)/ 并排打开配对 / 选区与选文字的区别

**1. 普通文本撤销失效 —— v0.17.0 引入的回归。**
`checkCallback` 里只判断了「有没有 PDF 视图在前台」。但**编辑批注文字时,前台视图
仍然是那个 PDF 视图**,于是 Cmd+Z 被这条命令认领,根本传不到光标所在的
contentEditable。加了第二个条件:`document.activeElement` 是
contentEditable / INPUT / TEXTAREA 时一律返回 false。**正在打字时,打字的撤销优先**
——它是此刻更直接的语义,而批注历史仍然可以从命令面板走。

**2. 新增「在右侧并排打开对应的译文/原文」命令。** 原来只有原地切换
(`switchToCounterpart`,保留阅读位置)。并排是配对的另一半用途,补上。会**复用已经
打开该文件的分栏**而不是每次再堆一个副本。

**3. 「选区」和「选文字」有没有本质区别 —— 有,但不在存储形式上。**
两者最终都归成同一个 `anchor` 矩形,渲染、高亮、箭头完全同一套代码。真正的区别是
**能不能被重新找回**:选中文字时我们还能拿到**文字本身**,存进 `quote` 之后,这个高亮
在重排渲染后、尤其是在**配对的另一种语言**上,还有东西可以用来重新定位;拖框只有坐标,
没有任何可供辨认的把手。

顺着这个区别改了一处实际行为:**用选中文字重新指定高亮时,现在会把选中的文字一并存为
`quote`**(之前无论哪种方式都把 quote 清成 undefined,等于主动扔掉了这个把手)。
拖框仍然只存坐标。同时清掉 `leaderAt`——它是**旧**框的比例,换了框就没有意义了。

`tsc` + `build` 通过。**三条都没在真实 Obsidian 里验证。** 第 1 条是回归修复,
建议优先试:在批注里打几个字然后 Cmd+Z,应该撤销的是你打的字,不是上一次批注操作。

### v0.20.0 —— 批注上只留一个控件,文字从最开头开始

用户:右上角那块特别大、白底把字挡住了;不如只留一个小按钮;长按拖动和点击能不能
合成一个;文字也别再空一行。三条其实是同一件事的三个面。

**为什么会变大:** 批注整体是 `transform: scale(zoom)` 的(v0.11.0),工具栏跟着一起
缩放是对的——但 v0.14.0 把图标从 1 个加到 5 个、再加上拖动手柄共 6 个,乘上放大倍数
之后就是页面上很扎眼的一块;而且**图标要在文字之上保持可读就必须有不透明背景**,
那块白底正好压在批注的第一行上。之前为此加过 `padding-top: 15px` 把正文整体下移——
等于**为了容纳工具栏,每条批注头上都空一行**。

**改法是把因去掉,而不是继续补偿:** 页面上的批注**只留一个控件**——
**拖动它移动批注,点击它打开菜单**。一个小记号不需要背板,所以白底整个删掉
(列表面板的行仍然保留背板:那里有好几个图标,而且底下没有文字要保护),
`padding-top` 也一并删掉,**正文回到左上角从第一个字开始**。原来那几个图标做的事
menu 里全都有,没有任何操作只能在工具栏做。

**一个控件两种手势**靠位移阈值区分(`TAP_SLOP_PX = 4`):按下后位移一直小于阈值就是
点击(开菜单、**不提交任何修改**);越过阈值才算拖动(此时才加 `is-dragging`、
才写位置)。注意"越过后又拖回原处"仍然算拖动而不是点击——用户明显是抓住了它,
松手弹出菜单会很突兀;这种情况提交一个零位移。

`tap.mjs` 覆盖:干净点击、手抖点击(阈值内)、真实拖动、拖出去又拖回来、恰好等于
阈值。全过。**没在真实 Obsidian 里点过。** 需要留意的是:手柄现在直接浮在文字上方
(只在悬停时显现),如果实际用起来仍然挡到第一行末尾的字,可以再把它挪到批注外侧。

### v0.21.0 —— 尺寸基准:既不是绝对像素,也不是"当前 PDF 的点",而是页面宽度占比

用户问:老论文 PDF 分辨率低,组件和字体特别大,应该按绝对分辨率还是按当前 PDF 大小?

**两个都不对,但问题问到了根子上。**

之前所有 chrome 尺寸(轨道宽度、间距、字号、手动微调)都以**该文档自己的 PDF 点**为
单位,乘以 `zoom`(= 渲染像素 / 点)。这里藏着一个错误假设:**PDF 点不是固定的物理
尺寸**,它只是文件 media box 里声明的数字。老扫描件经常把一页声明成常规尺寸的一个
零头,这种页面要填满同样的屏幕宽度就必须用大得多的 `zoom`,于是任何以"该文档的点"
为单位的东西都跟着放大——这就是"特别大"的全部原因。

- **绝对屏幕像素**(另一个候选)更糟:它会把 v0.11.0 解决的问题重新打开——缩放时批注
  不再与它所批注的正文保持相对大小。
- **按页面宽度归一化**同时满足两边:设置值现在表示**占页面宽度的比例**。
  新增 `PageBox.unit = 渲染宽度 / 612`(612 = US Letter,基本上所有论文的声明值),
  所有 chrome 尺寸从乘 `zoom` 改成乘 `unit`。「轨道宽度 220」在任何文档里都等于
  页宽的 220/612,无论文件把这个宽度叫 612 点还是 306 点;而在同一份文档内,
  放大页面时它照样跟着放大。

**顺带发现 `zoom` 字段已经是死代码**:锚点坐标转屏幕走的是纯比例
(`(pt - ptX0)/ptWidth * box.width`),压根不需要"每点多少像素"。既然唯一的用途已被
`unit` 取代,就把它删了,免得以后有人再拿它去量 chrome。

**设置值不需要迁移**:在常规的 612pt 页面上 `unit` 恰好等于原来的 `zoom`,
所以正常论文上的观感完全不变;变的只有那些 media box 异常的文件——而它们本来就是坏的。

`unit.mjs` 验证:三种差异极大的 media box(612 / 306 / 1224 点)在同样显示宽度下,
轨道占页宽的比例**完全相同**;同一文档内显示宽度翻倍时字号也翻倍;页宽为 0 或负数
不产生 NaN。另外加了一个**对照用例复现旧公式**:同一设置在三种 media box 下分别是
288 / 575 / 144 像素——正是用户看到的现象。全过。**没在真实 Obsidian 里看过。**

### v0.21.1 —— Markdown 里的撤销仍然失效:全局热键注册本身就是错的

v0.19.0 给撤销命令的 `checkCallback` 加了"不在 PDF 就返回 false"的判断,以为这样
就不会影响别处。**没用**——用户反馈 Markdown 里的文字撤销照样失效。

**判断条件不是问题所在,注册方式才是。** `addCommand({ hotkeys: [Mod+Z] })` 是一次
**全局**注册:它在 Obsidian 的热键表里为 `Mod+Z` 占了一个条目,而这个组合键正是编辑器
自己要用的。即使 `checkCallback` 在 Markdown 里老老实实返回 false,按键在到达编辑器
之前就已经被消费掉了——**声明这个热键本身就是冲突**,里面写什么判断都救不回来。

**改法是把这个能力搬到它该在的作用域**:不再声明任何默认热键,改为在**每个 PDF 视图
自己的 `containerEl` 上**挂 `keydown` 监听(`attachUndoKeys`,跟着该视图的
`Component` 一起注册和释放)。别处发生的键盘事件根本到不了这个元素,所以它**不可能**
影响 Markdown 编辑器——这不是"判断得更严",而是从结构上让冲突无从发生。命令仍然留在
命令面板里,只是不带默认热键,想要的话可以自己绑一个不冲突的。

原来那个"正在打字就让位"的判断保留了下来(在批注里打字时,撤销的是你打的字)。
另外顺手加了一条:没有可撤销的内容时**不拦截按键**,让它继续传下去,而不是拦下来再
弹一个"没有可撤销的批注修改"。

**这条是结构性修复,不是又一次调参**——之所以前一次没修好,正是因为我把它当成了
条件判断的问题。**没在真实 Obsidian 里验证**:请重点试 Markdown 里的 Cmd+Z。
